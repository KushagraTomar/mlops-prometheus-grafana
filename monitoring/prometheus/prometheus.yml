global:
  scrape_interval: 1s
  evaluation_interval: 1s # evaluates alerts rules every 1 second

# rule_files:
#   - "alerts.yml"

scrape_configs:
  - job_name: "ml-app"
    static_configs:
      - targets: ["localhost:8000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
